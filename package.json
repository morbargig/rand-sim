{
  "name": "rand-sim-5",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "babel-cli": "^6.26.0",
    "babel-node": "0.0.1-security",
    "babel-preset-env": "^1.7.0",
    "mongoose": "^5.13.15",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-masonry-component": "^6.2.1",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "concurrently \"nodemon server.js --exec babel-node --presets babel-preset-env\" \"react-scripts start\"",
    "dev": "concurrently \"react-scripts start\" \"nodemon server.js --exec babel-node --presets babel-preset-env \"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
